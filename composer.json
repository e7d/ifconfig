{
  "autoload": {
    "classmap": [
      "app/src/"
    ]
  },
  "require": {
    "geoip2/geoip2": "^2.10",
    "symfony/yaml": "^5.0"
  },
  "scripts": {
    "setup": "composer install && composer dump-autoload",
    "build:docker": "test -f .env && . ./.env && docker build --build-arg MAXMIND_LICENSE_KEY=$MAXMIND_LICENSE_KEY -t ifconfig ."
  }
}
