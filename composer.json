{
  "name": "e7d/ifconfig",
  "description": "Get you connection information.",
  "autoload": {
    "classmap": [
      "app/src/"
    ]
  },
  "require": {
    "geoip2/geoip2": "^2.10",
    "symfony/yaml": "^5.0"
  },
  "scripts": {
    "benchmark": "ab -k -c 100 -n 100000 http://localhost:5757/",
    "build:docker": "./hooks/build",
    "setup": "composer install && composer dump-autoload"
  }
}
